name: kangoos_core
description: KangoOS core engine — snippets, search, storage and LLM adapter.
version: 0.1.0
# repository: https://github.com/<org>/KangoOS

environment:
  sdk: '>=3.7.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  drift: ^2.28.2
  http: ^1.6.0
  path: ^1.9.0
  sqlite3: ^2.9.4

dev_dependencies:
  build_runner: ^2.4.15
  drift_dev: ^2.28.0
  lints: ^5.0.0
  test: ^1.24.0
