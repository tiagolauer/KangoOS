name: kangoos_core
description: Core engine do KangoOS — snippets, busca, storage e LLM adapter.
version: 0.1.0
# repository: https://github.com/<org>/KangoOS

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
